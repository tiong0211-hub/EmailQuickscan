# 런타임은 표준 라이브러리만으로 동작한다(사내망 pip 설치 불가 전제 —
# CLAUDE.md 규칙 8). 아래는 개발 환경에서 "있으면 더 잘 동작하는" 선택
# 가속기다. 설치하지 않아도 프로그램은 죽지 않고 폴백으로 동작한다.
#
#   pip install -r requirements.txt
#
# Windows 배포용 번들도 이 파일 그대로 packaging/build_windows.ps1이
# 빌드 머신에서 설치한다 — 컴파일러가 전혀 필요 없다(아래 win32 줄
# 참조, packaging/BUILD.md에 근거 설명).

libpff-python; sys_platform != "win32"
libpff-python-windows; sys_platform == "win32"
extract-msg
PyYAML
chardet
rich

# 개발 도구
pytest>=8
ruff
mypy
pyinstaller
